# Core (always required)
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
numpy==1.26.4
scipy==1.13.1
soundfile==0.12.1
httpx==0.27.2
python-multipart==0.0.12

# Phase 4 — librosa (primary engine, always required)
librosa==0.10.2

# Phase 6 — CREPE (pitch tracking — optional, install for intonation analysis)
# pip install crepe tensorflow
# crepe==0.0.13
# tensorflow==2.15.0

# Phase 6 — Spotify Basic Pitch (note detection — optional)
# pip install basic-pitch
# basic-pitch==0.3.2

# Phase 6 — Essentia (timbre & articulation quality — optional)
# Linux/Mac: pip install essentia
# Windows: pip install essentia-tensorflow (TF build) or use Docker
# essentia==2.1b6.dev1110
